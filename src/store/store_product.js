const mod_Product = {
  state: () => ({
    researchPoints: 100,
  }),
  getters: {
    get_productState(state) {
      return state;
    },
  },
  mutations: {},
  actions: {},
  modules: {},
};

export default mod_Product;
